id          = "projection/001-example"
op          = "projection"
description = "Synthetic projection fixture — declared companion_sections does NOT match derived."

[coverage]
tr_op = ["TR-OP-042"]
companion_sections = ["§99"]

[inputs]
view = "input-view.cbor"

[expected]
watermark = "expected-watermark.cbor"
