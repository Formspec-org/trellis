id          = "append/001-example"
op          = "append"
description = "Synthetic fixture-lint test vector — declared invariants mismatch."

[coverage]
tr_core    = ["TR-CORE-001"]
invariants = [99]

[inputs]
authored_event = "input-authored-event.cbor"

[expected]
canonical_event = "expected-canonical-event.cbor"

[derivation]
document = "derivation.md"
