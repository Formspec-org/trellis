id = "tamper/016-disclosure-profile-from-mismatch"
op = "tamper"
status = "active"
description = "Disclosure-profile axis sibling of tamper/002-transition-from-mismatch, landed to close the G-O-5 retroactive reopen from the 2026-04-23 design-doc audit. The tampered event is a `trellis.disclosure-profile-transition.v1` event whose `from_disclosure_profile = \"rl-profile-C\"` contradicts the deployment's initial posture declaration, which pins `disclosure_profile = \"rl-profile-A\"`. Core §19 step 6.b's state-continuity check is the failure site: the verifier tracks a shadow state from the initial declaration and observes `from_*` does NOT equal that state. Per §19 step 6.b this is a *localizable* failure: the transition-outcome record for the event records `continuity_verified = false` and `failures = [\"state_continuity_mismatch\"]`, and §19 step 9's `integrity_verified` conjunction drops to false. The signature is valid — the tamper is semantic, not cryptographic. scope_change = \"Orthogonal\" exercises Appendix A.5.3 step 4's dual-attestation rule positively (both `prior` and `new` authorities attest) so the attestation step passes while continuity fails."

[coverage]
tr_op = [
    "TR-OP-044",
]

[inputs]
ledger = "input-tampered-ledger.cbor"
tampered_event = "input-tampered-event.cbor"
signing_key_registry = "input-signing-key-registry.cbor"
initial_posture_declaration = "input-initial-posture-declaration.bin"
posture_declaration = "input-posture-declaration.bin"

[expected.report]
structure_verified = true
integrity_verified = false
readability_verified = true
tamper_kind = "state_continuity_mismatch"
failing_event_id = "fbc618588f9bc16b93367133d2842e5d54d34112125a25ac6b7a986de6ac188e"
idempotency_key = "tamper-transition-016"

[derivation]
document = "derivation.md"
