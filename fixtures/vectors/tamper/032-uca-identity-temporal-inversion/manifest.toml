id = "tamper/032-uca-identity-temporal-inversion"
op = "tamper"
status = "active"
description = "ADR 0010 §\"Verifier obligations\" step 4 violation: resolved identity-attestation event's `sequence` is not strictly less than `attested_event_position`. Identity at seq 0; attested_event_position = 0; 0 ≥ 0. Verifier flips `identity_resolved = false` and emits `user_content_attestation_identity_temporal_inversion`. Adversary intent: establish identity AFTER the attestation the identity allegedly justifies."

[coverage]
tr_core = [
    "TR-CORE-018",
    "TR-CORE-030",
    "TR-CORE-035",
    "TR-CORE-152",
    "TR-CORE-154",
]

[inputs]
ledger = "input-tampered-ledger.cbor"
tampered_event = "input-tampered-event.cbor"
signing_key_registry = "input-signing-key-registry.cbor"

[expected.report]
structure_verified = true
integrity_verified = false
readability_verified = true
tamper_kind = "user_content_attestation_identity_temporal_inversion"
failing_event_id = "d4fb96cdedf0728b3e13344dc0ac04625186de02d859f0fe3415c88ba929ed89"

[derivation]
document = "derivation.md"
