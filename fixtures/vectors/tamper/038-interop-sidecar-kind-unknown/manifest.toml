id          = "tamper/038-interop-sidecar-kind-unknown"
op          = "tamper"
status      = "active"
description = """Wave 25 — `interop_sidecar_kind_unknown`. Manifest lists one entry with `kind = "made-up-kind"` (not in the ADR 0008 closed registry of `scitt-receipt`, `vc-jose-cose-event`, `c2pa-manifest`, `did-key-view`). Phase-1 verifier emits `tamper_kind = "interop_sidecar_kind_unknown"` before any digest check runs. Anchors TR-CORE-164 (closed-registry gate)."""

[coverage]
tr_core = ["TR-CORE-164"]

[inputs]
export_zip = "input-export.zip"

[expected.report]
structure_verified   = false
integrity_verified   = false
readability_verified = false
tamper_kind          = "interop_sidecar_kind_unknown"
