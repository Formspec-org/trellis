id          = "tamper/027-interop-sidecar-populated-phase-1"
op          = "tamper"
status      = "active"
description = """Phase-1 interop sidecar lock-off. Manifest lists a populated
interop_sidecars entry; Phase-1 verifier MUST reject with
interop_sidecar_phase_1_locked per ADR 0008."""

[coverage]
tr_core = ["TR-CORE-145"]

[inputs]
export_zip = "input-export.zip"

[expected.report]
structure_verified   = false
integrity_verified   = false
readability_verified = false
tamper_kind          = "interop_sidecar_phase_1_locked"

[derivation]
document = "derivation.md"
