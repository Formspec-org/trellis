id          = "verify/007-export-001-inclusion-proof-mismatch"
op          = "verify"
status      = "active"
description = """Negative verify vector: inclusion proof mismatch. Takes \
`export/001-two-event-chain` and flips one bit in `020-inclusion-proofs.cbor`’s \
leaf_hash for index 0 (then re-signs the manifest with updated digests) so ZIP \
structure and manifest bindings remain consistent but Core §19 step 7.b fails \
for that proof (localizable). Expected: structure passes, integrity fails. \
TR-OP-041 (posture-tier declaration) is intentionally not claimed: this vector \
is scoped to the §19 step 7.b integrity surface; posture coverage lives on \
`verify/001` and on the declaration-doc lint."""

[coverage]
tr_core = [
    "TR-CORE-067",
]

[inputs]
export_zip = "input-export.zip"

[expected.report]
structure_verified   = true
integrity_verified   = false
readability_verified = true

[derivation]
document = "derivation.md"

