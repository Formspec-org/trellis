# Derivation — `verify/012-export-004-optional-anchor`

Happy-path verify vector for Core §19 step 8 optional external-anchor handling. Reuses `export/004-external-payload-optional-anchor` intact: payload bytes are present under `060-payloads/`, `external_anchor_required = false`, and no anchor-resolution failure is expected.
