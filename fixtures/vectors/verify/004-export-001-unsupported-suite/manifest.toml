id          = "verify/004-export-001-unsupported-suite"
op          = "verify"
status      = "active"
description = """Negative verify vector: unsupported signature suite. Takes \
`export/001-two-event-chain` and rewrites `000-manifest.cbor`'s COSE protected \
header `suite_id` to an unregistered value while re-signing the manifest so \
the signature bytes are structurally valid. Exercises Core §19 step 2.b fatal \
failure: unregistered / inconsistent `suite_id` ⇒ abort with \
`structure_verified = false`."""

[coverage]
tr_core = [
    "TR-CORE-067",
]

[inputs]
export_zip = "input-export.zip"

[expected.report]
structure_verified   = false
integrity_verified   = false
readability_verified = false

[derivation]
document = "derivation.md"

