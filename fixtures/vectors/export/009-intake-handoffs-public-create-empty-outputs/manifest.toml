id            = "export/009-intake-handoffs-public-create-empty-outputs"
op            = "export"
status        = "deprecated"
deprecated_at = "2026-04-28"
description   = """Renumbered to `export/013-intake-handoffs-public-create-empty-outputs` during Wave 21 close-commit (ADR 0005 erasure-evidence Stage 4-C). Slot `export/009` was reassigned to `export/009-erasure-evidence-inline` per ADR 0005 *Fixture plan*, while slots `export/010..012` are reserved by ADR 0007 cert-of-completion (`export/010`) and ADR 0008 interop-sidecar lock-off scaffolds (`export/011`, `export/012`). The active intake-handoffs vector lives at `export/013-intake-handoffs-public-create-empty-outputs`. This tombstone preserves the `export/009` prefix per `scripts/check-vector-renumbering.py` (R16); deprecated vectors are excluded from Rust + Python conformance replay and from coverage / invariant audits per `scripts/check-specs.py` F6."""
