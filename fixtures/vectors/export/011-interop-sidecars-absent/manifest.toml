id          = "export/011-interop-sidecars-absent"
op          = "export"
status      = "active"
description = """Canonical positive: interop-sidecars/ tree absent;
manifest omits interop_sidecars (ADR 0008 Phase-1 reservation discipline).
Reuses export/001 chain for the underlying bytes — the signal is that
Phase-1 verifier accepts absent interop_sidecars without error."""

[coverage]
tr_core = ["TR-CORE-145"]

[inputs]
ledger_state = "input-ledger-state.cbor"

[expected]
zip        = "expected-export.zip"
zip_sha256 = "a0822f1f902fc9917ad6d1908dba9376f9995f9718b4c4eb79336fffd5bbfebb"

[derivation]
document = "derivation.md"
