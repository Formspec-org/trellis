# Trellis Export (Fixture) — export/006-signature-affirmations-inline

WOS-T4 signature export fixture. `062-signature-affirmations.cbor` is a chain-derived catalog over a readable WOS `SignatureAffirmation` payload and `066-signed-acts.cbor` is the verifier-facing signing projection. Both are re-derived from signed chain records; neither creates new semantic authority.
