# Trellis Export — export/009-erasure-evidence-inline

Single genesis erasure-evidence event (from `tamper/017` event 0) plus `064-erasure-evidence.cbor` bound via `trellis.export.erasure-evidence.v1`.
