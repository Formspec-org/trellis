# Pending-invariants / pending-matrix-rows allowlist (R13/F5).
#
# This file lists rows that are *normatively authored* in the matrix and the
# core / companion prose, but whose fixture vectors land in a follow-up
# commit of the same change train. Listed-but-now-covered entries are
# errors (the lint forces cleanup); listed-and-uncovered is allowed.
#
# Discipline:
#   - Only used to keep multi-commit trains green between spec-deltas
#     (commit 1) and fixture-corpus (commit 4).
#   - Each entry MUST cite its train + the commit that lands the fixture.
#   - This file is a transit allowlist, NOT a permanent residue ledger.
#
# Wave 25 (c2pa-manifest activation; ADR 0008 Phase-2 unlock):
#   TR-CORE-163..166 normative in commit 1 (spec deltas);
#   fixtures land in commit 4 (`tamper/037..040`). Cleared in commit 4.
#
# ADR 0012 (rkaf:Finding anchoring binding):
#   TR-CORE-178 lands the verifier-obligation row + Core §6.7 registry row in
#   the same change train as the ADR; the fixture corpus
#   (append/NNN-rkaf-finding-{emission,optional-fields-omitted,freshness-reemission}
#   and the seven `tamper/NNN-rkaf-finding-*` vectors) is deferred to the
#   first concrete adopter (Studio compiler / BVR runtime / wos-server
#   validation emitter) per the CLAUDE.md "no stubs" rule. The matrix-row
#   `Status: Proposal — open` is the load-bearing posture; this allowlist
#   entry forecloses the silent-skip alternative while the Rust types and
#   vectors are deferred. Clear when the first adopter ships the corpus.

pending_matrix_rows = [
    "TR-CORE-178",
]
