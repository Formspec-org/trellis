# Pending-projection-drill allowlist.
#
# Parallel to _pending-invariants.toml but tracks matrix rows whose
# Verification column is `projection-rebuild-drill` rather than
# `test-vector`. The drill-coverage rule (R7) is wired in a later commit;
# this file ships today so authoring can record drill-row escapes.
#
# Schema:
#   pending_matrix_rows = ["TR-OP-..."]
#
# Drive this list toward empty. When empty, remove the file.

pending_matrix_rows = []
